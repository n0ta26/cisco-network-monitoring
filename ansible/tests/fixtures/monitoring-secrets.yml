---
# Non-production values used only to validate rendered configuration in CI.
monitoring_stack_snmp_auths:
  cisco_v3:
    version: 3
    username: ci-monitor-user
    security_level: authPriv
    password: ci-auth-password
    auth_protocol: SHA
    priv_protocol: AES
    priv_password: ci-privacy-password
